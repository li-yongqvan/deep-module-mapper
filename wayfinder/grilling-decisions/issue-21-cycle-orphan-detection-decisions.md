# Issue #21 模块级循环依赖/孤儿模块检测 — 决策归档

> 用途：为设计文档 §3 的「用户确认」决策提供可复核落档（评审 B2）。
> 关联文档：设计文档 `wayfinder/design-doc-issue-21-module-cycle-orphan-detection.md`；执行 handoff `wayfinder/handoff-issue-21-module-cycle-orphan-detection.md`。
> 评审状态：`design-doc-issue-21-module-cycle-orphan-detection-评审意见书.md` 有条件通过（2026-09-01）。

## D1-D5 决策表

| 编号 | 决策 | 定案 | 确认方式 |
|---|---|---|---|
| D1 | 检测粒度与视图？ | **重组视图·模块容器级（最大粒度）**；现实视图/功能视图不做。检测依据 = 真实代码聚合边（`computeAggregatedModuleEdges`），不是用户画的线。 | 用户确认（2026-09-01）：原话「不用管现实视图。这个循环检测只要去做模块那个层级就好了，也就是最大的那一级」；二次确认「对的，就是最大粒度」+ 检测依据用真实代码聚合边。 |
| D2 | 孤儿边界：一个模块容器只有指向第三方的边（只 import 第三方）、且无人依赖它——算孤儿吗？ | **不算，拆成三分类**：正常（与其它模块容器有 ≥1 条边）/ 孤儿（与其它模块容器**且**第三方都无边的真孤立）/ **仅连第三方**（无模块边、有第三方边 → 单独高亮 + 说明功能） | 用户确认（2026-09-01）：原话「我认为这种需要单独高亮显示，然后说明功能」+ 确认「对，三分类」。 |
| D3 | 检测出的环在重组画布怎么呈现？ | **只标节点 + Inspector**：环成员模块容器加标记，**不画环边**（尊重 #18 D1 零边）；点节点 → Inspector 展示 SCC 成员集合 + 成员之间每段聚合边的 `rawEdges` 证据。 | 用户确认（2026-09-01）：选择「只标节点+Inspector（推荐）」。 |
| D4 | 用户画下构成环的那条边时，是否当场提示「构成了循环依赖」？ | **不做画线时提示**：#21 只做检测 + 标记；#18 画线交互保持原样（只校验边真实）。 | 用户确认（2026-09-01）：选择「不做画线时提示（推荐）」。 |
| D5 | 除了节点标记 + Inspector，要不要加汇总总览？ | **轻量汇总计数**：工具栏「N 个环 · M 个孤立 · K 个仅连第三方」，可点开列表定位节点。 | 用户确认（2026-09-01）：选择「轻量汇总计数（推荐）」。 |

## B1 后续裁决（2026-09-01 plan-review 后用户确认）

| 编号 | 决策 | 定案 | 确认方式 |
|---|---|---|---|
| B1 | v1 是否展示 `ModuleFinding.evidence.path`（环路径）？ | **不展示**。v1 `ModuleFinding` 移除 `path` 字段；Inspector 只展示 SCC 成员集合 + 成员之间所有聚合边证据。环路径作为后续迭代候选。 | 用户确认（2026-09-01）：回复「B」。 |

## 评审采纳记录（design-doc-issue-21-module-cycle-orphan-detection-评审意见书，2026-09-01 有条件通过）

| 评审项 | 结论 | 采纳落地 |
|---|---|---|
| B1 `evidence.path` 算法未裁决 | 阻塞 → 用户选 B | 设计文档 §5.1 移除 `path`；§5.4/§8 同步改为「成员集合 + 边证据」；§9 Q1 改为「已裁决」 |
| B2 范围收窄决策未落档 | 阻塞 → 采纳 | 本文件（D1-D5 + B1 + 评审采纳记录） |
| I1 Toolbar 可点列表交互 | 重要 → 采纳 | 设计文档 §5.3 新增 Toolbar 交互小节；§8 新增 Toolbar 定位测试 |
| I2 节点边框色/badge 落点 | 重要 → 采纳 | `containerStyle(moduleDiagnostic)` 函数化、badge 在 header 第一行、severity 映射实/虚线 |
| I3 Inspector 环证据渲染结构 | 重要 → 采纳 | §5.4 给出环/孤儿/仅连第三方三类渲染草图 |
| I4 活更新去抖策略 | 重要 → 采纳 | v1 不去抖，依赖 useMemo 即时重算 |
| I5 `severity` 字段消费 | 重要 → 采纳 | severity 决定 badge/描边实虚：error=实线红、warning=虚线灰/琥珀 |
| I6 `deriveNodes` 签名变更 | 重要 → 采纳 | §8 新增既有用例更新说明；实现时可选参数 + 默认值 |
| I7 `diagnostic` 命名冲突 | 重要 → 采纳 | 字段/类型统一改为 `moduleDiagnostic`，避免与 parser `Diagnostic` 混淆 |
| I8 测试覆盖缺口 | 重要 → 采纳 | §8 新增 Toolbar 定位、RecomposeCanvas 活更新、空输入边界、badge 更新测试 |
| S1 补不变量 | 建议 → 采纳 | §7 新增 #12（byModule 不含第三方节点）、#13（检测输出不写回 design） |
| S2 性能注记 | 建议 → 采纳 | §8 新增性能注记（200 模块阈值 / Web Worker 候选） |
| S3 空输入返回值 | 建议 → 采纳 | §5.1 明确空输入返回全空 findings |
| S4 description fallback | 建议 → 采纳 | §5.4  Inspector 仅连第三方功能说明增加空描述 fallback |
