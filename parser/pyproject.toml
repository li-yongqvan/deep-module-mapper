[project]
name = "deep-module-mapper-parser"
version = "0.1.0"
description = "AST-based codebase parser for the Deep Module Mapper."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
