# Ubiquitous Language

> Deep Module Mapper（模块地图工具）术语表。项目目录：`agent panel/deep-module-mapper/`。

## 核心域（模块与接口）

| Term | Definition | Aliases to avoid |
|---|---|---|
| **代码库** | 被分析的本地项目目录，包含源代码与配置。 | 项目、repo |
| **模块** | 代码库中一个自包含的实现单元，由**实现**和**端口**组成。 | 组件、包、文件 |
| **实现** | 模块内部隐藏的代码细节，不对外展示。 | 内部、逻辑 |
| **端口** | 模块对外暴露的入口集合（公开函数、类、导出符号），即模块与外界的连接面。 | 接口、API、出口 |
| **接口** | 端口的**使用说明书**：说明这个模块有哪些功能、如何调用。一个模块通常只有一个接口。 | API、契约、函数列表 |
| **接口功能** | 接口说明书里列出的单个能力条目（如某个公开函数或类）。 | 函数、方法、端点 |
| **依赖** | 一个模块的端口使用了另一个模块的端口所构成的关系。 | 引用、调用、关联 |
| **依赖边** | 可视化图中连接两个模块的箭头，表示一条**依赖**。 | 箭头、连线、边 |

## 视图域（用户界面）

| Term | Definition | Aliases to avoid |
|---|---|---|
| **现实视图** | 界面一：展示代码库当前真实模块、端口、依赖与深度分的视图。 | 真实图、当前图 |
| **自定义画布** | 界面二：用户挑选模块、自主连接依赖、设计理想架构的空白画布。 | 设计图、理想图 |
| **节点** | 可视化图上的一个模块图形（圆角矩形）。 | 方块、四边形、模块框 |
| **端口把手** | 节点上用于连接依赖边的小圆点/小方块，代表一个模块的输入或输出端口。 | 连接点、锚点 |
| **深度分** | 衡量模块"深/浅"的量化分数，由接口大小、扇入扇出、复杂度等综合计算。 | 健康分、质量分 |

## 质量域（深模块理念）

| Term | Definition | Aliases to avoid |
|---|---|---|
| **深模块** | 接口小而精、实现厚的模块：暴露面窄，内部承担复杂职责。 | 健康模块、好模块 |
| **浅模块** | 接口大而散、实现薄的模块：暴露面过宽，职责不聚焦。 | 坏模块、问题模块 |
| **架构契约** | 用户或 AI 定义的"哪些依赖被允许/禁止"的规则集合，用于评审理想设计。 | 规则、约束 |
| **AI 评审** | 由云端模型对用户在自定义画布上设计的依赖关系进行结构化评估。 | 评估、检查 |

## 基础设施域（工具与 AI）

| Term | Definition | Aliases to avoid |
|---|---|---|
| **解析器** | 后端中读取代码库、提取模块/端口/依赖的组件。 | 分析器、扫描器 |
| **本地模型** | 运行在本地（Ollama）的轻量模型，负责起草模块描述等低消耗任务。 | Ollama、小模型 |
| **云端模型** | 通过 API 调用的远程大模型，负责依赖评审等高质量推理任务。 | 大模型、API 模型 |
| **关卡** | 工具建造过程中的阶段性任务，有前置依赖，必须逐步解锁；成品使用阶段不存在关卡。 | 阶段、里程碑 |

## Relationships

- 一个**模块** 由 **实现** 和 **端口** 组成；**接口** 是 **端口** 的使用说明书。
- 一个**模块** 通过 **端口** 与另一个**模块** 产生 **依赖**；可视化时表现为一条 **依赖边**。
- **现实视图** 反映代码库的真实结构；**自定义画布** 反映用户的理想设计；两者通过 **AI 评审** 和 **架构契约** 做对比。
- **深模块** 接口小、实现厚；**浅模块** 接口大、实现薄。**深度分** 低 = 浅，高 = 深。
- **关卡** 只存在于规划与建造过程，最终**模块地图**工具的使用阶段没有关卡。

## Example dialogue

> **Dev:** "这个 `server` 模块的**深度分**为什么只有 3.1？"
>
> **领域专家:** "因为它的**端口**暴露了 5 个公开函数，但每个函数底下的**实现**都很薄，属于典型的**浅模块**。"
>
> **Dev:** "那我把几个相关函数合并成一个类，会不会变成**深模块**？"
>
> **领域专家:** "对——只要**接口**变小了、**实现**变厚了，**深度分**就会上去。但要看其它模块对它的**依赖**有没有变复杂。"
>
> **Dev:** "我在**自定义画布**上把 `tools` 连到 `memory`，让 AI 评审一下这条**依赖边**是否合理。"
>
> **领域专家:** "可以。云端模型会根据**架构契约**和简洁性原则来判断这条依赖是不是太绕。"

## Flagged ambiguities

- **"模块" vs "接口" vs "端口"**：三者不能混用。**模块** = 实现 + 端口；**端口** = 对外暴露的连接面；**接口** = 端口的使用说明书。口语中容易把"接口"当"函数"用，规范后"接口功能"才是单个函数/类。
- **"关卡" 的双义**：在 Deep Module Mapper 建造过程中，**关卡** = 有前置依赖的任务阶段；用户容易误以为工具本身也有关卡，必须强调成品使用阶段无关卡。
- **"节点" 不要叫"方块"或"四边形"**：虽然形状是圆角矩形，但术语统一叫**节点**。
